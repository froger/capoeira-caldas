---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/home/Hero.astro';
import Programs from '../../components/home/Programs.astro';
import Benefits from '../../components/home/Benefits.astro';
import SchedulePreview from '../../components/home/SchedulePreview.astro';
import Testimonials from '../../components/home/Testimonials.astro';
import InstructorsPreview from '../../components/home/InstructorsPreview.astro';
import HomeCTA from '../../components/home/HomeCTA.astro';
import { getSite } from '../../lib/data';
import { absoluteUrl, webPageSchema } from '../../lib/seo';

const locale = 'en';
const site = getSite(locale);
const siteOrigin = Astro.site ?? 'http://127.0.0.1:4321';
const path = '/en/';
const pageUrl = absoluteUrl(path, siteOrigin);
const siteUrl = absoluteUrl('/', siteOrigin);

const jsonLd = [
  webPageSchema({
    title: site.name,
    description: site.description,
    url: pageUrl,
    locale,
    siteName: site.name,
    siteUrl,
  }),
];
---

<BaseLayout title={site.name} description={site.description} locale={locale} path={path} jsonLd={jsonLd} ogImage="/images/hero-poster.jpg">
  <Hero locale={locale} />
  <Programs locale={locale} />
  <Benefits locale={locale} />
  <SchedulePreview locale={locale} />
  <Testimonials locale={locale} />
  <InstructorsPreview locale={locale} />
  <HomeCTA locale={locale} />
</BaseLayout>
